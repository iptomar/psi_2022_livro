@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <div class="dropdown">
          <span>
            <a>
                <img style=" width: 40px; height: 40px; object-fit:fill; border-radius: 20px" src="~/Logos/logo_user2b.png"/>
            </a>
          </span>
          <div class="dropdown-content">
            <form  class="form-inline">
                <button  style="width: 100%; text-align: left" type="submit" class="nav-link btn btn-link text-dark">Profile</button>
            </form>
            <form  class="form-inline">
                <button style="width: 100%; text-align: left" type="submit" class="nav-link btn btn-link text-dark">Edit</button>
            </form>
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button style="width: 100%; text-align: left" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
          </div>
        </div>
    </li>
    @*<li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>*@
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
    <a>
        <img style=" width: 40px; height: 40px; object-fit:fill; border-radius: 20px" src="~/Logos/logo_user2a.png"/>
    </a>
}
</ul>
